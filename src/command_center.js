const ship = require('./ship');

class CommandCenter {
    constructor(x,y){
        this.x = x; 
        this.y = y;
    }

    isLost(ship){
        // if it is negatively out of bounds
        
    }
}

module.exports = {
    CommandCenter
}